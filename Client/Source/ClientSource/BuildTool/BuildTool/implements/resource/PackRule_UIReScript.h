#pragma once

#include "IPackRule.h"

namespace rsc
{
	/*class PackRule_UIReScript : public IPackRule
	{
	public:
		PackRule_UIReScript(const Path& srcWorkdir,
							const Path& dstWorkdir, 
							SyncLoggerPtr loggerPtr);

		virtual ~PackRule_UIReScript();

	protected:
		virtual bool _Packing() const override;
	};*/
}