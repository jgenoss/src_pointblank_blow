#pragma once

#include "IPackRule.h"

namespace rsc
{
	/*class PackRule_String : public IPackRule
	{
	public:
		PackRule_String(const Path& srcWorkdir, 
						const Path& dstWorkdir, 
						SyncLoggerPtr loggerPtr);
		
		virtual ~PackRule_String();

	protected:
		virtual bool _Packing() const override;
	};*/
}