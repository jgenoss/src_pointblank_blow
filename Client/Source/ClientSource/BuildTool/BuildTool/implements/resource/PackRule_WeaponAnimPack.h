#pragma once

#include "IPackRule.h"

namespace rsc
{
	/*class PackRule_WeaponAnimPack : public IPackRule
	{
	public:
		PackRule_WeaponAnimPack(const Path& srcWorkdir, 
								const Path& dstWorkdir,
								SyncLoggerPtr loggerPtr);
		
		virtual ~PackRule_WeaponAnimPack();

	protected:
		virtual bool _Packing() const override;
	};*/
}