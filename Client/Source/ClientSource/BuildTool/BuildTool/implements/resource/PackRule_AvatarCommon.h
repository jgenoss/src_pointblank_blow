#pragma once

#include "IPackRule.h"

namespace rsc
{
	/*class PackRule_AvatarCommon : public IPackRule
	{
	public:
		PackRule_AvatarCommon(const Path& srcWorkdir,
							  const Path& dstWorkdir, 
							  SyncLoggerPtr loggerPtr);

		virtual ~PackRule_AvatarCommon();

	protected:
		virtual bool _Packing() const override;
	};*/
}