#pragma once

#include "IPackRule.h"

namespace rsc
{
	/*class PackRule_UIReScene : public IPackRule
	{
	public:
		PackRule_UIReScene(const Path& srcWorkdir,
						   const Path& dstWorkdir, 
						   SyncLoggerPtr loggerPtr);

		virtual ~PackRule_UIReScene();

	protected:
		virtual bool _Packing() const override;
	};*/
}